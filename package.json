{
  "name": "karakurum-silkrute-traders-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --prefix client && npx prisma generate --schema=server/prisma/schema.prisma",
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\"",
    "kill-node": "taskkill /IM node.exe /F",
    "check-node": "tasklist | findstr node",
    "clean": "rimraf client/dist server/dist client/node_modules server/node_modules node_modules package-lock.json client/package-lock.json server/package-lock.json"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": "24.x"
  }
}